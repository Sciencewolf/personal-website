# Optional. A fine-grained personal access token with no scopes, used only to raise the
# GitHub API rate limit from 60 to 5000 requests/hour. Public profile and repo data is
# fetched without authentication if this is left unset.
GITHUB_TOKEN=

# GitHub username whose public profile and repositories are shown. Defaults to Sciencewolf.
GITHUB_USERNAME=Sciencewolf
