[tool.vercel]
entrypoint = "api:app"

[project]
name = "personal-website-backend"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "Flask==3.1.0",
    "flask-cors==5.0.0",
    "requests==2.32.3",
    "python-dotenv==1.0.1"
]